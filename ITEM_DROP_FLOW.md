# 道具掉落与拾取流程

道具获取不经过角色升级池。当前来源与交互规则如下：

```text
精英怪死亡
  → 按掉落概率生成一件可用道具
  → 玩家触碰掉落物
  → 被动 / 进化材料 / 未满槽主动：直接获得，不暂停战斗
  → 主动槽已满且是新主动：暂停战斗，比较 Q/E 槽后替换或放弃

稀有晶脉开采完成
  → 暂停战斗
  → 从当前可用道具与进化材料中严格三选一
  → 若选中的新主动需要替换，继续保持暂停直至完成决定

Boss 击败
  → 本局立即胜利
  → 胜利宝箱三选一，永久解锁下一局的掉落内容
```

职责边界：

- `domain/items/ItemDropPool` 只负责从可用候选中随机选择，支持注入随机源。
- `domain/items/ItemInventory` 负责双主动槽、无限被动、等级和替换判定。
- `systems/ItemDropSystem` 只负责世界掉落实体、标签、碰撞拾取。
- `ui/ItemPickupToast` 提供不暂停战斗的独立拾取反馈。
- `ui/ActiveItemReplacement` 展示新旧主动道具的等级与效果对比。
- `application/RunSession` 编排暂停、恢复、奖励来源和武器进化材料交付。

开发环境可使用 `?qaItemReplacement` 快速验证双主动槽已满后的替换流程。
